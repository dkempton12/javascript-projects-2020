**Modal** that opens upon **button click events**, and closes by clicking outside of the **inner modal element**.


The **inner modal** makes use of **innerHTML template literals** to append elements to the modal.